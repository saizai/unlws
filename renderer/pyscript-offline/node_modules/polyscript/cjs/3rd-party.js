'use strict';
/* c8 ignore start */

// toml
const toml = async (text) => (
  await import(/* webpackIgnore: true */'./3rd-party/toml.js')
).parse(text);
exports.toml = toml;

// zip
const zip = () => import(/* webpackIgnore: true */'./3rd-party/zip.js');
exports.zip = zip;

/* c8 ignore stop */
